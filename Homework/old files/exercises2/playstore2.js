function playstore2(input) {
    const peturBudget = Number(input[0]);
    const vcCount = Number(input[1]);
    const procesorCount = Number(input[2]);
    const ramCount = Number(input[3]);
    const vc = 250
    let procesor = 0
    let ram = 0
    if ( vcCount > procesorCount ) {
        
    }
}