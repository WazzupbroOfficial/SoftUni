function solve(input) {
    let index = 0
    let k = Number(input[index++])
    let l = Number(input[index++])
    let m = Number(input[index++])
    let n = Number(input[index++])

    let kNumbers = k
    let lNumbers = l
    let mNumbers = m
    let nNumbers = n

    for (kNumbers; kNumbers <= 8; kNumbers++) {
        if (kNumbers % 2 == 0){
            
        }
    }
    kNumbers--

    for (lNumbers; lNumbers <= 9; lNumbers++) {
        if (lNumbers % 2 != 0) {
            
        }
    }
    lNumbers--

    for (mNumbers; mNumbers <= 8; mNumbers++) {
        if (mNumbers % 2 == 0){
            
        }
    }
    mNumbers--

    for (nNumbers; nNumbers <= 9; nNumbers++) {
        if (nNumbers % 2 != 0) {
            
        }
    }
    nNumbers--

    let firstNumber = kNumbers.toString()+lNumbers.toString()
    let secondNumber = mNumbers.toString()+nNumbers.toString()

    if (firstNumber === secondNumber){
    
    }
   
    


   }
solve([7,
    6,
    8,
    5,
    ])